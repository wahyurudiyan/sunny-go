// Code generated by sgo. DO NOT EDIT.

package redis

import (
	"context"
	"fmt"
	"os"

	goredis "github.com/redis/go-redis/v9"
)

func envOr(key, fallback string) string {
	if v := os.Getenv(key); v != "" {
		return v
	}
	return fallback
}

// Connect opens a connection to Redis, configured via REDIS_HOST and
// REDIS_PORT (defaults match docker/docker-compose.yml).
func Connect(ctx context.Context) (*goredis.Client, error) {
	addr := envOr("REDIS_HOST", "localhost") + ":" + envOr("REDIS_PORT", "6379")

	client := goredis.NewClient(&goredis.Options{Addr: addr})

	if err := client.Ping(ctx).Err(); err != nil {
		return nil, fmt.Errorf("failed to connect to redis: %w", err)
	}

	return client, nil
}
