// Code generated by sgo. DO NOT EDIT.

package {{.Package}}

// DomainEvent is the marker every event {{.AggregateName}} can record
// satisfies — a sealed interface (the unexported method can only be
// implemented in this package), so PullEvents can return a slice of a
// common type without falling back to `any`.
type DomainEvent interface{ isDomainEvent() }
{{range .DomainEvents}}
type {{.Name}} struct {
{{- range .Fields}}
	{{.GoName}} {{.GoType}} `json:"{{.Name}}"`
{{- end}}
}

func ({{.Name}}) isDomainEvent() {}
{{end}}
{{range .ValueObjects}}
// {{.Name}} is a Value Object — treat it as immutable: construct a new
// one rather than mutating a field in place.
type {{.Name}} struct {
{{- range .Fields}}
	{{.GoName}} {{.GoType}} `json:"{{.Name}}"`
{{- end}}
}
{{end}}
{{range .ChildEntities}}
type {{.Name}} struct {
{{- range .Fields}}
	{{.GoName}} {{.GoType}} `json:"{{.Name}}"`
{{- end}}
}
{{end}}
type {{.Aggregate.Name}} struct {
{{- range .Aggregate.Fields}}
	{{.GoName}} {{.GoType}} `json:"{{.Name}}"`
{{- end}}

	events []DomainEvent
}

// PullEvents returns every event recorded since the last call (by an
// owned {{.Aggregate.Name}} method in aggregate.go) and clears them —
// call this after successfully persisting, then hand the result to an
// EventPublisher.
func (a *{{.Aggregate.Name}}) PullEvents() []DomainEvent {
	events := a.events
	a.events = nil
	return events
}
