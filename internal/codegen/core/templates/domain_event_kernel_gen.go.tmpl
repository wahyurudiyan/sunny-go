// Code generated by sgo. DO NOT EDIT.

// Package event holds the one thing every entity's domain events need
// in common, so an EventPublisher can accept events from any of them
// (ARCHITECTURE.md §17). A sealed marker interface (an unexported
// method) only works within a single package; DomainEvent needs to
// cross package boundaries — from internal/domain/<entity> to
// internal/application/ports — so it uses an exported EventName method
// instead. Lives under internal/domain (not internal/application) so
// the dependency direction stays domain -> nothing,
// application -> domain, never the reverse.
package event

// DomainEvent is any event a domain package can record. Every
// generated event struct implements it via EventName.
type DomainEvent interface {
	EventName() string
}
