package {{.Package}}

// Add domain methods, validation, and invariants for {{.Entity}} here.
//
// This file is created once by `sgo generate code` and never overwritten
// by later runs — see ARCHITECTURE.md §6.
