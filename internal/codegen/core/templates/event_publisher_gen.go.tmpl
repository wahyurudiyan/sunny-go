// Code generated by sgo. DO NOT EDIT.

// Package ports holds application-layer ports shared across every
// entity — EventPublisher today (ARCHITECTURE.md §17).
package ports

import (
	"context"

	event "{{.EventImportPath}}"
)

// EventPublisher is what an application service hands a
// {{.AggregateExample}}'s PullEvents() result to after a successful save.
// A real message-bus implementation (Kafka/RabbitMQ/NATS) is a future,
// separate phase — this interface is the seam it plugs into; NoopEventPublisher
// is the only implementation wired into wire_gen.go until then.
type EventPublisher interface {
	Publish(ctx context.Context, events ...event.DomainEvent) error
}

// NoopEventPublisher discards every event — a real, working default so
// a generated project runs with zero configuration, the same "always
// runnable" promise the in-memory persistence adapter already makes
// (Decision #15).
type NoopEventPublisher struct{}

func (NoopEventPublisher) Publish(context.Context, ...event.DomainEvent) error { return nil }
