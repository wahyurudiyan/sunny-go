// Code generated by sgo. DO NOT EDIT.

// Package mask holds the one masking primitive every entity's
// obfuscate_visible-marked field needs (ARCHITECTURE.md §22), shared
// across entities the same way internal/domain/event is.
package mask

// Obfuscate returns value with its first visible characters kept and
// the rest replaced by a fixed-length mask, regardless of how long the
// real remainder actually is — a mask proportional to the remaining
// length (one "*" per hidden character) would itself leak the value's
// real length, which is exactly what this is meant to avoid. A visible
// count longer than value reveals the whole thing (nothing left to
// mask); a negative one is treated as 0 (fully masked).
func Obfuscate(value string, visible int) string {
	if visible < 0 {
		visible = 0
	}
	if visible > len(value) {
		visible = len(value)
	}
	return value[:visible] + "*****"
}
